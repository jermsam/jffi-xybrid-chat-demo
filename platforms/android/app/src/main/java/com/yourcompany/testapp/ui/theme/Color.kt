package com.yourcompany.testapp.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Custom Palette
val PrimaryIndigo = Color(0xFF6366F1)       // Indigo 500
val PrimaryDarkIndigo = Color(0xFF4F46E5)   // Indigo 600
val SecondaryTeal = Color(0xFF14B8A6)       // Teal 500

// Dark Mode Colors
val DarkBg = Color(0xFF0B0F19)              // Ultra-dark slate blue
val DarkSurface = Color(0xFF161E2E)         // Rich dark surface
val DarkOnBg = Color(0xFFF3F4F6)
val DarkOnSurface = Color(0xFFE5E7EB)

// Light Mode Colors
val LightBg = Color(0xFFEEF0F5)             // Soft lavender-gray (WhatsApp-like)
val LightSurface = Color(0xFFFFFFFF)        // Pure white for bars
val LightOnBg = Color(0xFF111827)
val LightOnSurface = Color(0xFF1F2937)

// Chat bubble colours — light mode
val LightUserBubble = Color(0xFF4F46E5)          // Rich Indigo for user
val LightBotBubble = Color(0xFFFFFFFF)            // White for bot (on gray bg)
val LightUserBubbleText = Color(0xFFFFFFFF)
val LightBotBubbleText = Color(0xFF111827)

// Chat bubble colours — dark mode
val DarkUserBubble = Color(0xFF6366F1)            // Indigo for user
val DarkBotBubble = Color(0xFF1E293B)             // Dark slate for bot
val DarkUserBubbleText = Color(0xFFFFFFFF)
val DarkBotBubbleText = Color(0xFFF1F5F9)
